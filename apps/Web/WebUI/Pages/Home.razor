@page "/"
@inject NavigationManager Navigation

// Redirigir a la WelcomeScreen en vez de mostrar la landing directamente
protected override void OnInitialized()
{
    Navigation.NavigateTo("/welcome", true);
}
